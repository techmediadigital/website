{
    "name": "fullstack-admin-site-root",
    "version": "1.0.0",
    "scripts": {
        "start": "cd backend && npm start",
        "build": "cd frontend && npm install && npm run build",
        "vercel-build": "npm run build",
        "dev": "concurrently \"npm run dev --prefix frontend\" \"npm run dev --prefix backend\""
    },
    "dependencies": {
        "body-parser": "^2.2.1",
        "cors": "^2.8.5",
        "express": "^5.2.1",
        "jsonwebtoken": "^9.0.3"
    },
    "devDependencies": {
        "concurrently": "^9.2.1"
    }
}